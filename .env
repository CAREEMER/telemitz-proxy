# Домен вашего прокси
PROXY_DOMAIN=proxy.yourdomain.com

# URL вашего backend API
BACKEND_API_URL=https://api.telemitz.net

# Публичный IP этого сервера (обязательно для TURN)
# Узнать: curl -s ifconfig.me
EXTERNAL_IP=YOUR_PUBLIC_IP

# === Секреты ===

# Секрет для связи Nginx -> Backend API (X-Internal-Secret header)
# Должен совпадать с PROXY_API_SECRET в backend-go
API_SECRET=local_dev_proxy_secret

# Секрет для TURN аутентификации
# Должен совпадать с TURN_SECRET в backend-go (для генерации credentials)
# Backend генерирует: username=timestamp:userId, credential=HMAC-SHA1(TURN_SECRET, username)
# Coturn проверяет credentials используя этот же секрет
TURN_SECRET=local_dev_proxy_secret
